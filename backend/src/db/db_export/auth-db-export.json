{
    "type": "database",
    "name": "auth",
    "objects": [
        {
            "type": "table",
            "database": null,
            "name": "users",
            "withoutRowId": false,
            "strict": false,
            "ddl": "CREATE TABLE users (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      name TEXT NOT NULL,\n      email TEXT NOT NULL UNIQUE,\n      password TEXT NOT NULL,\n      role TEXT NOT NULL DEFAULT 'user' CHECK(role IN ('user', 'admin')),\n      failedLoginAttempts INTEGER NOT NULL DEFAULT 0,\n      lockUntil INTEGER,\n      resetPasswordToken TEXT,\n      resetPasswordExpire INTEGER,\n      refreshToken TEXT,\n      createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,\n      updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP\n    )",
            "columns": [
                { "name": "id", "type": "INTEGER", "constraints": [{ "type": "PRIMARY KEY", "definition": "PRIMARY KEY AUTOINCREMENT" }] },
                { "name": "name", "type": "TEXT", "constraints": [{ "type": "NOT NULL", "definition": "NOT NULL" }] },
                { "name": "email", "type": "TEXT", "constraints": [{ "type": "NOT NULL", "definition": "NOT NULL " }, { "type": "UNIQUE", "definition": "UNIQUE" }] },
                { "name": "password", "type": "TEXT", "constraints": [{ "type": "NOT NULL", "definition": "NOT NULL" }] },
                { "name": "role", "type": "TEXT", "constraints": [{ "type": "NOT NULL", "definition": "NOT NULL DEFAULT 'user' " }, { "type": "CHECK", "definition": "CHECK(role IN ('user', 'admin'))" }] },
                { "name": "failedLoginAttempts", "type": "INTEGER", "constraints": [{ "type": "NOT NULL", "definition": "NOT NULL DEFAULT 0" }] },
                { "name": "lockUntil", "type": "INTEGER" },
                { "name": "resetPasswordToken", "type": "TEXT" },
                { "name": "resetPasswordExpire", "type": "INTEGER" },
                { "name": "refreshToken", "type": "TEXT" },
                { "name": "createdAt", "type": "DATETIME", "constraints": [{ "type": "DEFAULT", "definition": "DEFAULT CURRENT_TIMESTAMP" }] },
                { "name": "updatedAt", "type": "DATETIME", "constraints": [{ "type": "DEFAULT", "definition": "DEFAULT CURRENT_TIMESTAMP\n    " }] }
            ],
            "rows": []
        }
    ]
}