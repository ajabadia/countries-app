/**
 * Representa una entidad de Continente.
 */
export interface Continent {
  id: string; // Código del continente (ej. 'EU')
  defaultname: string; // Nombre del continente en inglés
  // Otros campos específicos de Continent si los hubiera
}