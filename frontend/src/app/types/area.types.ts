/**
 * Representa una entidad de Área.
 */
export interface Area {
  id: string; // Código del área (ej. '001')
  defaultname: string; // Nombre del área en inglés
  // Otros campos específicos de Area si los hubiera
}